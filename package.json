{
  "name": "@pkgjs/action",
  "private": true,
  "version": "0.0.0-development",
  "description": "Github Actions tooling for testing Node.js packages",
  "scripts": {
    "lint": "npx -- eslint .",
    "test": "for PRIVATE_ACTION in private-actions/*/; do cd ${INIT_CWD}/${PRIVATE_ACTION} && npm test; done"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pkgjs/action.git"
  },
  "author": "@pkgjs",
  "license": "MIT",
  "engines": {
    "node": "^16 || ^14 || ^12"
  },
  "devDependencies": {
    "@hapi/eslint-plugin": "^5.1.0",
    "eslint": "^7.32.0"
  }
}
